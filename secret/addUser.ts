/*
    Secret api to add user
*/
import { keya, keyb, plat, body, adduser } from './key';

export default async function registerAUser(username :string, avatar :number, description :string) {

}

async function firstFetch(time :string) {
    
}

async function register (username :string, avatar :number, description :string, hash :any) {
 
}
